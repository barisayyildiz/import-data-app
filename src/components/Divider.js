const Divider = ({ height = 2, color = "#DCE2F0" }) => {
  return (
    <div
      style={{
        minHeight: height,
        backgroundColor: "#DCE2F0",
      }}
    ></div>
  );
};

export default Divider;
